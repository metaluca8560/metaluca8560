{
  "name": "cancer-basics-miniapp",
  "private": true,
  "version": "1.0.0",
  "description": "암이란 무엇인가 — 암 기초 교양 · 앱인토스 미니앱 패키지",
  "type": "module",
  "scripts": {
    "web:dev": "node sync.mjs && vite --port 5181",
    "web:build": "node sync.mjs && vite build",
    "dev": "ait dev",
    "build": "ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^2.10.6"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
