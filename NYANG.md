# 냥이동 — 클라우드 명부(Supabase) 설정 가이드

기본 상태의 냥이동 주민증 발급소(`nyang-village.html`)는 **로컬 데모 모드**로 동작합니다.
(이름 해시로 주민번호 발급, 이 브라우저에만 저장 · 인구 집계 없음)

이 가이드를 따라 하면 **실제 전입 순서대로 주민번호 발급 + 전체 인구 실시간 집계 +
1,000명 자동 마감**이 되는 클라우드 모드로 바뀝니다. Supabase 무료 요금제로 충분합니다.

## 설정 (5분)

### 1. Supabase 프로젝트 만들기

1. [supabase.com](https://supabase.com) 가입 → **New project** (무료)
   - 클로페이용 프로젝트가 이미 있다면 그대로 같이 써도 됩니다 (테이블이 겹치지 않음)
2. 왼쪽 메뉴 **SQL Editor** → 이 저장소의 `nyang-schema.sql` 내용 전체를
   붙여넣고 **Run** → "Success" 확인
   - 실행하면 촌장단(1번 푸름이 · 2번 루릭)이 자동으로 전입되어 있습니다

### 2. 키 두 개 복사

**Settings → API** 에서:

| 값 | 어디에 넣나 |
|----|-------------|
| Project URL (`https://xxxx.supabase.co`) | `nyang-config.js`의 `supabaseUrl` |
| `anon` `public` 키 | `nyang-config.js`의 `supabaseAnonKey` (공개돼도 괜찮은 키) |

`service_role` 키는 냥이동에는 필요 없습니다.

### 3. 배포

`nyang-config.js` 커밋/푸시 → 배포 반영 → 페이지 상단에
"현재 전입 주민 N / 1,000명"이 뜨면 클라우드 모드입니다.

## 동작 방식

```
전입신고 → nyang_move_in RPC: 이름 검사 → 1,000명 마감 확인
          → 잠금 후 다음 번호 발급 (동시 전입에도 번호 안 겹침)
같은 이름 재신고 → 기존 번호·고양이 종류 그대로 재발급
페이지 로드 → nyang_stats RPC: 인구 + 최근 전입 5명 표시
```

- 브라우저는 명부를 **읽기만** 할 수 있고(RLS), 쓰기는 RPC로만 가능합니다.
- 명부에는 닉네임·고양이 종류·주민번호만 저장됩니다 (개인정보 없음).

## 자주 묻는 것

- **데모 모드로 돌아가고 싶다** — `nyang-config.js`의 두 값을 비우면 됩니다.
- **1,000명이 다 찼다** — 전입 버튼이 자동으로 "모집 마감"으로 바뀝니다.
  2기를 열려면 한도를 늘리거나(`nyang_move_in`의 `1000`) 새 동네 테이블을 만드세요.
- **명부 전체 보기** — Supabase 대시보드 → Table Editor → `nyang_residents`.
