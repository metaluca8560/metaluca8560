# 이 파일은 `npm run build:test-ads` 로 빌드할 때만 읽혀요.
# 평소 배포 빌드( `npm run build`, `npm run web:build` )는 이 파일을 전혀 보지 않기
# 때문에, 아래 테스트 ID가 실제 제출용 결과물에 섞일 일이 없어요.
#
# 앱인토스 공식 문서가 제공하는 "테스트 배너 광고 ID"예요. 실제 광고 그룹 ID가
# 아니고, 심사 제출용 빌드에 절대 들어가면 안 돼요.
VITE_AIT_AD_TEST_ID=ait-ad-test-banner-id
