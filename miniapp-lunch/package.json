{
  "name": "lunch-picker-miniapp",
  "private": true,
  "version": "1.0.0",
  "description": "오늘 점심 뭐먹지? — 앱인토스 미니앱 패키지",
  "type": "module",
  "scripts": {
    "web:dev": "node sync.mjs && vite --port 5180",
    "web:build": "node sync.mjs && vite build",
    "dev": "ait dev",
    "build": "ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^2.10.6"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
