# 배포 대상 이름은 반드시 유지할 것.
# 시장·타로 등 여러 앱이 이 주소를 직접 가리키고 있어서,
# 이름을 바꾸면 그 앱들이 옛 Worker를 계속 보게 된다.
name = "small-recipe-9345"
main = "worker.js"
compatibility_date = "2026-01-01"

[vars]
ALLOW_ORIGIN = "*"
