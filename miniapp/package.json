{
  "name": "salary-analyzer-miniapp",
  "private": true,
  "version": "1.0.0",
  "description": "급여명세서 AI 해석기 — 앱인토스 미니앱 패키지",
  "type": "module",
  "scripts": {
    "web:dev": "node sync.mjs && vite --port 5173",
    "web:build": "node sync.mjs && vite build",
    "dev": "ait dev",
    "build": "ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^2.10.6"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
